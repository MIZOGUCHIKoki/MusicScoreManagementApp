<h1>UsersController#show</h1>
<ul>
    <li><%= @user.name %></li>
    <li><%= @user.email %></li>
</ul>
<%= link_to "編集する", edit_user_path(@user) %>
<% if @user.scores.any? %>
    <ol>
        <%= render @scores %>
    </ol>
    <%= will_paginate @scores%>
<% end %>